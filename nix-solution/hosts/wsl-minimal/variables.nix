# nix-solution/hosts/wsl-minimal/variables.nix
{
  # You can use the same username or a different one.
  # The flake.nix will default to "wsluser" if this file
  # or this line is missing.
  username = "wsluser";
}
